services:
  youngest-server:
    container_name: youngest-server
    user: "1000:1000"
    build: .
    ports:
      - "8080:80"
    volumes:
      - ./data:/data
    environment:
      LOGGING_LEVEL: DEBUG
